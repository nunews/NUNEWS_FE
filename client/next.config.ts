import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // 추후에 이미지 경로 특정되면 아래와 같은 식으로 경로 추가하겠습니다.
  /* config options here */
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'cdn.sportalkorea.com',
  //       pathname: '/news/photo/**',
  //     },
  //   ],
  // },
};
export default nextConfig;
